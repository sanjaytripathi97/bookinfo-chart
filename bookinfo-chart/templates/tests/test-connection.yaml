apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bookinfo-chart.fullname" . }}-test-connection"
  labels:
    {{- include "bookinfo-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bookinfo-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
